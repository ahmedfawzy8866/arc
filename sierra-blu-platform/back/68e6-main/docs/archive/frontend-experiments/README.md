# Frontend experiment archive

These files are preserved as Sierra Blu donor/reference material only.

They are intentionally not the production source of truth. The canonical public landing implementation lives at `app/page.tsx`, and `/landing` reuses that page.
